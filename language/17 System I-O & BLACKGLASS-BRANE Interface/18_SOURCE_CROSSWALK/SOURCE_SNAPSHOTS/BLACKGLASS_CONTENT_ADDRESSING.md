# Content Addressing

Use SHA-256 CAS for raw source and immutable artifacts.

```text
source_vault/sha256/ab/abcdef....
```

Occurrence metadata lives separately so identical bytes can have multiple historical occurrences without duplicating evidence content.
