# Python Reference Emulator

Package: `genesis_chirality_machine`

The emulator implements the GCHI and ISA-0 contracts using Python standard-library facilities and `mmap`.

It is intentionally dependency-light so Codex or a fresh recovery environment can run it immediately.
