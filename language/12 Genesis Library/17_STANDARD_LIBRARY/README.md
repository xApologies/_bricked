# Reference local registry

This tree is executable test/reference package content for Section 12. `example.qftleg` is a reference non-stdlib dependency used to validate package-to-package composition.
