# Portal -> BRANE M^5 Re-lift

After closure the destination Geometric is re-adapted to BRANE's organizational contract:

```text
I = canonical MMO identity + new instance + source instance
D = source dependencies + Portal ID + Corridor path witness
Chi = destination fabric region + content root + chirality route class
R = inherited recursive depth + Portal lifecycle/history
P = inherited provenance + route/capacity/closure receipts
```

BRANE still owns realization `Z`. Section 04 does not equate scientific 3+1+1 axes with BRANE's organizational five-coordinate contract.
