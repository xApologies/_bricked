# Corridor Mathematics Crosswalk

| Working mathematical role | Section-04 implementation role |
|---|---|
| `Address_a(p)` | `PortalAddress` + payload characterization |
| `L_a` | source composite-state lift/read |
| `W_ab^3` | `CorridorPath` witness (software model only) |
| `P_W` | route-conditioned re-realization transaction |
| `Pi_b` | destination segment + target address closure |
| chirality transport `tau` | edge chirality-class admission and recorded trajectory |
| `Inv` | field-selected invariant residue root |
| `R_n >= R_min` | path minimum Resolution check |
| `B_n >= B_min` | path minimum Bandwidth/capacity check |
| `R_c` | destination readback + closure predicate |

This is an implementation mapping. It does not upgrade the working corridor mathematics into a physical interconnect claim.
