# Versioning
`1.0.0` denotes the first frozen Genesis computational-language contract. `1.x` may add compatible libraries, backends, diagnostics, and optimizations. Any semantic reinterpretation of a frozen construct, ABI-breaking contract change, or relaxation of required safety/provenance obligations requires `2.0` or an explicitly versioned ABI boundary.
