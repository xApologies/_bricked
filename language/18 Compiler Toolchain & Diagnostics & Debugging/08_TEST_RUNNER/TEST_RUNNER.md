# Test runner

A `.gtest.json` case declares source, optional seeded SOURCE endpoint files, expected status, optional expected register names, optional expected receipt operations, and optional expected build id. Tests run through the same build + traced runtime path as ordinary programs. Expected compiler failures can be asserted by diagnostic code.
