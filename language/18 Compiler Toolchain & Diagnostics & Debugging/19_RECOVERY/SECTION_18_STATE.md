# Section 18 recovery state

Section 18 adds the development-tooling plane: deterministic build bundles, diagnostics, source maps, trace/replay debugger, test runner, and reproducibility checker. The Python package `genesis_toolchain` is the current reference implementation. Section 17's `genesis_system_io` package is bundled unchanged as the immediate executable dependency.
