# Diagnostic contract

Diagnostics use stable symbolic codes inherited from the failing semantic layer whenever available. Every diagnostic has severity, code, message, source file, optional span, optional note list, and a deterministic diagnostic id. Human rendering is a projection of the machine-readable diagnostic object.
