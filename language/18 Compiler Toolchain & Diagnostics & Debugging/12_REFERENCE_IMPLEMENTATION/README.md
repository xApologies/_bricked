# Reference implementation

Set `PYTHONPATH=12_REFERENCE_IMPLEMENTATION` and invoke `python -m genesis_toolchain doctor`.

`genesis_system_io` is copied unchanged from Section 17 Part A so this core package can execute its own toolchain tests without relying on host-global modules.
