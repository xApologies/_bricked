# Build manifest

The normalized manifest records `build_id`, `source_digest`, `program_id`, target/profile, toolchain version, verifier proof root, instruction count, and artifact digests. Runtime run ids and wall-clock metadata, if an implementation chooses to emit them, are non-semantic and must not participate in the build id.
