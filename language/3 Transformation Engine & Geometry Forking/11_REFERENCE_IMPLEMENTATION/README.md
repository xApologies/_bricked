# Reference implementation

`genesis_transform_engine` is the Section 03 Python oracle. It runs above the Section 01/02 vendored reference modules and implements immutable Geometric forks.

Run tests from this directory with:

```bash
python -m unittest discover -s ../12_TESTS -p 'test_*.py' -v
```
