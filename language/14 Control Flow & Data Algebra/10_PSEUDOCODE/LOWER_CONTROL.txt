LOWER_IF(cond): BRANCH -> then / else; each branch compiles in child lexical scope; both jump to end.
LOWER_MATCH(sum): emit DATA_IS per case, branch to case body, optionally DATA_PAYLOAD binder, jump to end; require exhaustiveness.
LOWER_REPEAT(N): require 0<=N<=4096; compile body N times with fresh child scope.
