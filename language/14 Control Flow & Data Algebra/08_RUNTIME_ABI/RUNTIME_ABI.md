# Runtime ABI
The Python emulator uses tagged dictionaries for records/variants. Those wire shapes are emulator artifacts. The semantic ABI is the typed immutable value contract, which future native backends may represent differently.
