# Effect-order barrier

The ordered list of effectful node IDs before optimization must equal the ordered list after optimization. Section 11 v0.1 does not attempt commutativity proofs between effect classes.
