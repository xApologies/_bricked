# Quantum barriers

`Q_PREPARE`, `Q_SUPERPOSE`, `Q_ENTANGLE`, `Q_CHANNEL`, and `Q_MEASURE` retain order and identity. Generic `MOVE` coalescing refuses QSTATE values. Measurement cannot be moved, duplicated, folded, or erased.
