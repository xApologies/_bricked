# GIR analysis model

The reference analysis computes producers, consumers, data dependencies, explicit dependency/effect edges, exports, effect sets, effectful node order, quantum node order, Portal/Road node order, bridge order, provenance order, and graph hashes. These witnesses are recorded before and after each optimization run.
