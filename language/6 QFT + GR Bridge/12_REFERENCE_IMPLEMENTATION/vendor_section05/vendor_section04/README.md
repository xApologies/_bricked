# Reference Implementation

`genesis_portal_engine` is a Python oracle for Section 04. It vendors the Section-03 reference stack under `vendor_section03/` and implements constrained Corridor routing, capacity reservation, full Geometric re-realization, closure receipts, and BRANE re-lift.

Run tests from `15_TESTS` with the Section-04 reference implementation on `PYTHONPATH`.
