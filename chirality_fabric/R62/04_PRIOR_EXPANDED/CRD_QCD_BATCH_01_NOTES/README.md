# CRD -> QCD Inheritance Live Model v1.9 QMO-Lite

This is the first source-corrected **cumulative live-model checkpoint** after MASTER_2 invalidated the provisional C6 basin-cycle route.

Authoritative active facts:

- structural basins are hard-stop values `0.142857, 0.2857, 0.42857, 0.57, 0.7, 0.857`;
- the infinite `142857...` grammar belongs to the containing environment/runtime grammar, not to a six-state Road cycle;
- L08 C6->A2 and L09B C6 compact lift are retired, preserved only as provenance;
- L09A static CRD Z2 -> restricted SU(3) gauge-orbit result is retained;
- active target is L08B: type the actual Road->Helicon selector with no imposed QCD mapping;
- finish line is L09E: a typed CRD->QCD map plus gauge-invariant physicality and naturality/imitation verdict.

Use `API/mini_qmo_api.py` or query `DATABASE/inheritance.sqlite3` directly.


## v1.9 major break

L08B closed by typing `S_RH` as a partial, generally set-valued Road→Helicon spectral correspondence. A unique single-mode selector is not source-derived. L08C is now active.


## v1.9 major break

L08C closed the exact quotient-descent criterion and exposed the missing Road/Helicon→Persistent-Boundary carrier map. Direct six-basin→three-color projection is rejected. L09C is active.


## v1.9 major break

L09C closed: Time-Space reaches a general QFT-compatible handoff but does not inherit rank three or SU(3). L09C1 is active and requires an explicit residue-derived reduction witness `W_3`.


## v1.9 source correction

Chirality Corridor was recovered from Rainbow Road source canon. It closes the Road-only Persistent-Boundary carrier on the corridor-backed sector. The remaining carrier gap is specifically Helicon-mode decoration/readout into the boundary/QFT residue. L09C no-go remains valid; L09C1 stays active.


## v1.9 major break

L09C1 is upstream-blocked by the source-declared missing Time-Shell→Q handoff. `Lambda_HQ` is now the explicit construction target. L09D stays locked.


## v1.9 major break

L09A2 closes an exact independent theorem: fixed-defect CRD motion is gauge fixing, while an unbinding that changes the seam cohomology class changes the embedded-Z2 gauge orbit and a closed-loop parity invariant. The sector remains abelian. The principal project blocker remains L08D1.
