# Recovery Prompt — CRD -> QCD Live Model v1.3

Mount this package as the authoritative current work-layer state.

Do not resurrect the v1.2 C6 basin cycle. MASTER_2 is authoritative for basin semantics: hard-stop structural basin values; environmental recurring 142857 grammar is separate.

Retain L06 no-go, L07 target algebra only, L08A source reset PASS, L09A exact static gauge skeleton PASS. Treat L09B as retired.

Resume at **L08B**: derive/type `S_RH` from actual Rainbow Road/Portal + chirality + History + basin telemetry into Helicon. Then L08C audits invariant residue under the Time-Space/QFT projection. Only after that may L09C ask whether an SU(3) reduction is source-supported.

The current finish line is L09E. Do not unlock glueball/confinement until L09E closes positively.

Any major theorem, no-go, source correction, new typed map, or roadmap remap must produce a new cumulative checkpoint with updated SQLite state and supersession ledger.


## v1.4 recovery delta

L08B is CLOSED.

Use the source-closed type:

`S_RH : R_adm ⇉ H_Hel`

where `H_Hel={H_alpha | L_H(H_alpha)=lambda_alpha H_alpha}`.

Do not manufacture a unique Helicon mode. Basin telemetry and Road History remain retained input/witness data, but no direct map from them to spectral mode index is source-closed.

Active run: **L08C — Invariant-Residue Projection Audit**.


## v1.5 recovery delta

L08C is CLOSED.

Use:
`Pi:dXi->M=dXi/G_Pi`
and
`F_p=Inv(Pi^-1(p))`.

A Road/Helicon feature survives only by quotient descent:
`a = a_bar o Pi`, equivalently `G_Pi`-invariance / constancy on projection fibers.

The typed carrier
`iota_partial : Graph(S_RH) ⇀ dXi`
is not source-closed.

Do not interpret `Pi` as `B6->C3`.

Active run: **L09C — QFT Handoff -> Rank-3 / SU(3) Reduction Gate**.


## v1.6 recovery delta

L09C is CLOSED:
- QFT compatibility PASS.
- Source-inherited rank-3 / SU(3) reduction NO-GO.

Do not set the QFT bundle rank to three by assumption.

Required witness:
`W_3=(E_c,rho_c,h_c,omega_c,nabla_c)`.

Active run: **L09C1 — QCD Reduction Witness Typing and Source Search**.
L09D remains locked.


## v1.7 recovery delta

Source correction: Portal is a transaction over an admissible Chirality Corridor.

Use:
`L_alpha : Sigma_alpha -> dXi_alpha`,
`P_W : dXi_alpha -> dXi_beta`,
`C_alpha_beta = Pi_beta o P_W o L_alpha`.

Thus a corridor-backed Road has a source-supported terminal Persistent-Boundary carrier.

Do **not** retain the older statement that the entire Road→boundary carrier is open.

Still open:
- Helicon-mode decoration/readout into boundary/QFT residue;
- G_Pi descent of non-residue address fields;
- QCD reduction witness W_3.

Active run remains **L09C1**.


## v1.8 recovery delta

L09C1 is CLOSED / BLOCKED UPSTREAM.

Target:
`Lambda_HQ : D_HQ ⇀ Q_loc`.

Do not map a Helicon one-form directly to an SU(3) connection by analogy.

Principal blocker: **L08D1 — Constructive Time-Shell→Q Handoff**.


## v1.9 recovery delta

L09A2 is CLOSED EXACT.

Use:
`J_{f,x}(e)=eta^(f(e)+delta x(e))`.

Gauge-orbit classification:
`J_{f,x} ~ J_{f',x'} iff [f]=[f'] in H^1(G;Z2)`.

Therefore:
- fixed-`f` reorientation = gauge fixing;
- unbinding with changed `[f]` = gauge-invariant orbit transition;
- restricted sector remains abelian, so QCD remains open.

Principal blocker remains **L08D1 — Constructive Time-Shell→Q Handoff**.
